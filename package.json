{
  "name": "telegram-contest",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "dev": "parcel src/index.html && cp src/chart_data.json dist/",
    "build": "parcel build src/index.html --no-source-maps --public-url=./ && cp src/chart_data.json dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^1.12.1"
  }
}
